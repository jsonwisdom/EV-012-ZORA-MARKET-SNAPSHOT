name: Verify EV Receipts

on:
  push:
  pull_request:

jobs:
  verify-ev:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify EV-012
        run: bash _truth/tools/verify_ev.sh EV-012
